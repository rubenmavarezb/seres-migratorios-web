---
/**
 * `/artistas` — index of the 14 people of the collective (SM-036). Shared body
 * in `internos/paginas/Artistas.astro` (SM-048), where the JSDoc of the two
 * OBSERVACIONES of this page lives; this file only fixes the locale.
 */
import PlantillaArtistas from '../../components/internos/paginas/Artistas.astro';
import type { Idioma } from '../../i18n/utils.ts';

/** This file only ever renders under the default (Spanish) locale. */
const idioma: Idioma = 'es';
---

<PlantillaArtistas idioma={idioma} />
