---
/**
 * `/ediciones` — the collective's edition archive (SM-039). Its body lives in
 * `internos/paginas/Ediciones.astro`, shared with the English twin
 * `/en/editions` (SM-048); this file only fixes the locale.
 */
import Ediciones from '../../components/internos/paginas/Ediciones.astro';
import type { Idioma } from '../../i18n/utils.ts';

/** This file only ever renders under the default (unprefixed) locale. */
const idioma: Idioma = 'es';
---

<Ediciones idioma={idioma} />
