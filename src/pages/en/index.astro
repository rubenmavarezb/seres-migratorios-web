---
/**
 * `/en/` — English home, twin of `/` (SM-034), replacing the Fase 1 test page.
 * Shared body in `internos/paginas/Home.astro` (SM-048); this file only fixes
 * the locale, so both routes render the same structure with the same dictionary
 * keys and `en.json` instead of `es.json`.
 */
import Home from '../../components/internos/paginas/Home.astro';
import type { Idioma } from '../../i18n/utils.ts';

/** This file only ever renders under the `/en/` locale. */
const idioma: Idioma = 'en';
---

<Home idioma={idioma} />
